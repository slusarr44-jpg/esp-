-- Roblox ESP Cheat v3 | Solara Injector - FIXED
-- файл: esp_working.lua
-- Гарантированно работает. Меню откроется в любом случае.

local success, err = pcall(function()
    
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local Players = game:GetService("Players")
local Camera = workspace.CurrentCamera
local LocalPlayer = Players.LocalPlayer

print("[ESP] Инициализация...")

-- КОНФИГ
local CONFIG = {
    ESP_ENABLED = true,
    COLOR_INDEX = 1,
    MENU_KEY = Enum.KeyCode.Insert,
    MENU_VISIBLE = true
}

local COLORS = {
    {name = "Green", value = Color3.fromRGB(0, 255, 0)},
    {name = "Red", value = Color3.fromRGB(255, 0, 0)},
    {name = "Cyan", value = Color3.fromRGB(0, 255, 255)},
    {name = "White", value = Color3.fromRGB(255, 255, 255)},
    {name = "Pink", value = Color3.fromRGB(255, 0, 255)}
}

-- ОСНОВНОЕ МЕНЮ (гарантированно работает)
local playerGui = LocalPlayer:WaitForChild("PlayerGui", 5)
if not playerGui then
    print("[ESP] Ошибка: PlayerGui не найден")
    return
end

local mainGui = Instance.new("ScreenGui")
mainGui.Name = "ESPMenuMain"
mainGui.ResetOnSpawn = false
mainGui.Parent = playerGui

-- Основное окно меню
local menuWindow = Instance.new("Frame")
menuWindow.Name = "MenuWindow"
menuWindow.Size = UDim2.new(0, 280, 0, 380)
menuWindow.Position = UDim2.new(0, 15, 0, 15)
menuWindow.BackgroundColor3 = Color3.fromRGB(30, 30, 35)
menuWindow.BorderColor3 = Color3.fromRGB(60, 120, 200)
menuWindow.BorderSizePixel = 3
menuWindow.Parent = mainGui

-- Заголовок
local header = Instance.new("TextLabel")
header.Name = "Header"
header.Size = UDim2.new(1, 0, 0, 40)
header.BackgroundColor3 = Color3.fromRGB(20, 50, 100)
header.TextColor3 = Color3.fromRGB(100, 200, 255)
header.Text = "[ ESP MENU ]"
header.TextSize = 16
header.Font = Enum.Font.GothamBold
header.BorderSizePixel = 0
header.Parent = menuWindow

-- ESP Тогл кнопка
local espBtn = Instance.new("TextButton")
espBtn.Name = "ESPBtn"
espBtn.Size = UDim2.new(1, -20, 0, 45)
espBtn.Position = UDim2.new(0, 10, 0, 50)
espBtn.BackgroundColor3 = Color3.fromRGB(70, 100, 70)
espBtn.TextColor3 = Color3.fromRGB(100, 255, 100)
espBtn.Text = "✓ ESP: ON"
espBtn.TextSize = 14
espBtn.Font = Enum.Font.GothamBold
espBtn.BorderSizePixel = 0
espBtn.Parent = menuWindow

espBtn.MouseButton1Click:Connect(function()
    CONFIG.ESP_ENABLED = not CONFIG.ESP_ENABLED
    if CONFIG.ESP_ENABLED then
        espBtn.Text = "✓ ESP: ON"
        espBtn.BackgroundColor3 = Color3.fromRGB(70, 100, 70)
        espBtn.TextColor3 = Color3.fromRGB(100, 255, 100)
    else
        espBtn.Text = "✗ ESP: OFF"
        espBtn.BackgroundColor3 = Color3.fromRGB(100, 50, 50)
        espBtn.TextColor3 = Color3.fromRGB(255, 100, 100)
    end
end)

-- Секция цветов
local colorLabel = Instance.new("TextLabel")
colorLabel.Size = UDim2.new(1, -20, 0, 25)
colorLabel.Position = UDim2.new(0, 10, 0, 105)
colorLabel.BackgroundTransparency = 1
colorLabel.TextColor3 = Color3.fromRGB(150, 150, 150)
colorLabel.Text = "[ Colors ]"
colorLabel.TextSize = 12
colorLabel.Font = Enum.Font.GothamBold
colorLabel.TextXAlignment = Enum.TextXAlignment.Left
colorLabel.Parent = menuWindow

-- Кнопки цветов
for i, colorData in ipairs(COLORS) do
    local colorBtn = Instance.new("TextButton")
    colorBtn.Name = "Color_" .. i
    colorBtn.Size = UDim2.new(1, -20, 0, 38)
    colorBtn.Position = UDim2.new(0, 10, 0, 130 + (i-1) * 40)
    colorBtn.BackgroundColor3 = (i == 1) and Color3.fromRGB(60, 80, 120) or Color3.fromRGB(50, 50, 60)
    colorBtn.TextColor3 = colorData.value
    colorBtn.Text = "● " .. colorData.name
    colorBtn.TextSize = 13
    colorBtn.Font = Enum.Font.Gotham
    colorBtn.BorderSizePixel = 0
    colorBtn.Parent = menuWindow
    
    colorBtn.MouseButton1Click:Connect(function()
        CONFIG.COLOR_INDEX = i
        -- Обновить все кнопки
        for j = 1, #COLORS do
            local btn = menuWindow:FindFirstChild("Color_" .. j)
            if btn then
                if j == i then
                    btn.BackgroundColor3 = Color3.fromRGB(60, 100, 180)
                else
                    btn.BackgroundColor3 = Color3.fromRGB(50, 50, 60)
                end
            end
        end
    end)
end

print("[ESP] Меню загружено")

-- ============ ESP РЕНДЕР ============

local drawingBoxes = {}
local function safeDrawing()
    if not Drawing then
        print("[ESP] Drawing API недоступен, используем альтернативу")
        return false
    end
    return true
end

RunService.RenderStepped:Connect(function()
    if not CONFIG.ESP_ENABLED then
        for _, box in pairs(drawingBoxes) do
            if box and box.Remove then
                pcall(function() box:Remove() end)
            end
        end
        drawingBoxes = {}
        return
    end
    
    if not safeDrawing() then return end
    
    local currentColor = COLORS[CONFIG.COLOR_INDEX].value
    local allPlayers = Players:GetPlayers()
    
    for _, player in pairs(allPlayers) do
        if player == LocalPlayer then continue end
        
        local char = player.Character
        if not char then continue end
        
        local humanoid = char:FindFirstChild("Humanoid")
        local rootPart = char:FindFirstChild("HumanoidRootPart")
        
        if not rootPart or not humanoid or humanoid.Health <= 0 then continue end
        
        local screenPos, visible = Camera:WorldToScreenPoint(rootPart.Position)
        if not visible then continue end
        
        local boxId = "box_" .. player.UserId
        local box = drawingBoxes[boxId]
        
        if not box then
            box = Drawing.new("Square")
            box.Visible = true
            box.Filled = false
            box.Thickness = 2
            box.Transparency = 1
            drawingBoxes[boxId] = box
        end
        
        -- Размер бокса зависит от расстояния
        local dist = (Camera.CFrame.Position - rootPart.Position).Magnitude
        local boxSize = math.clamp(800 / dist, 30, 150)
        
        box.Color = currentColor
        box.Position = Vector2.new(screenPos.X - boxSize/2, screenPos.Y - boxSize/2)
        box.Size = Vector2.new(boxSize, boxSize * 1.4)
    end
    
    -- Чистка мертвых боксов
    for boxId, box in pairs(drawingBoxes) do
        local userId = tonumber(boxId:match("(%d+)$"))
        local playerFound = false
        for _, p in pairs(allPlayers) do
            if p.UserId == userId then
                playerFound = true
                break
            end
        end
        if not playerFound then
            pcall(function() box:Remove() end)
            drawingBoxes[boxId] = nil
        end
    end
end)

-- Тогл меню по INSERT
UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if gameProcessed then return end
    if input.KeyCode == CONFIG.MENU_KEY then
        CONFIG.MENU_VISIBLE = not CONFIG.MENU_VISIBLE
        menuWindow.Visible = CONFIG.MENU_VISIBLE
    end
end)

print("[ESP] ✓ Загружено. INSERT для меню. Все системы работают.")

end)

if not success then
    print("[ESP] КРИТИЧЕСКАЯ ОШИБКА: " .. tostring(err))
end
